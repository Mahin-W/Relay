---
phase: adhoc — 7-tab payroll spreadsheet expansion
task: 0 of 8
status: research_complete_ready_to_plan
last_updated: 2026-04-10T18:15:40.428Z
---

<current_state>
Research phase 100% complete. All 7 parallel research agents finished and both source files fully read.
NO code written yet. NO tests written yet. NO plan written yet.
Paused immediately after research, before the planning/TDD phase.
</current_state>

<completed_work>

- Task 0 (Research): ALL 7 parallel agents completed
  - Read src/payroll/spreadsheetGenerator.js (402 lines) — full structure understood
  - Read src/tests/unit/spreadsheetGenerator.test.js (327 lines) — 23 existing tests catalogued
  - Read src/payroll/payCalculator.js + payDb.js — exact data shapes known
  - Read src/schedule/generateSchedule.js + reviewSchedule.js — publish-time data known
  - Read src/reliability/reliabilityScore.js + reliabilityDb.js — both exist and work
  - Read supabase-schema.sql — critical: 3 required tables missing (see decisions)
  - Read package.json — exceljs ^4.4.0, NO chart libraries
  - Read scripts/recalc.py — understands JSON output format
</completed_work>

<remaining_work>

- Task 1: Invoke superpowers:writing-plans → write full implementation plan
- Task 2: Write ALL tests first in spreadsheetGenerator.test.js (TDD red phase, node --test → fail)
- Task 3: Implement all 7 tabs in spreadsheetGenerator.js using parallel agents for tab logic
- Task 4: Run node --test → all green
- Task 5: Run python scripts/recalc.py on generated file → zero formula errors
- Task 6: simplify src/payroll/spreadsheetGenerator.js
- Task 7: superpowers:verification-before-completion
- Task 8: git add -A && git commit -m "feat: expanded 7-tab payroll spreadsheet"
</remaining_work>

<decisions_made>

## Critical schema findings (affects implementation):
- `time_off_requests` table does NOT exist in supabase-schema.sql → Tab 7 always shows graceful empty state, no DB query needed
- `schedule_receipts` table does NOT exist → Tab 6 Attendance can't show confirmation status from DB
- `payroll_records` table does NOT exist → Tab 3 4-week comparison always shows current week only with "History builds over time" note

## Design decisions:
- `/mnt/skills/public/xlsx/SKILL.md` does not exist — use ExcelJS patterns from existing code
- No chart libraries → Tab 4 hours bar chart = cell-fill based (color-fill cells proportional to hours)
- All new params backward-compatible: `coverageEvents=[], reliabilityData=[], timeOffData=[], budgetData=null, revenueData=null`
- Old "Late Arrivals Log" standalone sheet is REMOVED — late data merges into Tab 6 (Attendance & Reliability)
- Existing test `'"Late Arrivals Log" NOT created when lateEvents empty'` must be UPDATED (new design always creates 7 sheets)
- All other 22 existing tests must still pass (Schedule, Payroll sheets still exist with same names)

## Tab order (final):
1. Dashboard (new)
2. Schedule (existing, enhanced)
3. Payroll Detail (existing, expanded)
4. Hours Analysis (new)
5. Coverage Log (new)
6. Attendance & Reliability (new — absorbs old Late Arrivals Log)
7. Time Off (new — always empty state, no DB table exists)
</decisions_made>

<blockers>
None. Research complete, ready to implement.
</blockers>

<context>
The task came in as a large local command (/gsd:pause-work was triggered mid-research-read). 
All research was done, both key files were fully read into context. The plan was to:
1. Use superpowers:writing-plans to write a detailed plan
2. Write tests first (TDD), run node --test to confirm red
3. Implement using parallel subagents for tab logic (Agent A: tabs 1-2, Agent B: tabs 3-4, Agent C: tabs 5-7)
4. Assemble in main file, run recalc.py, verify, simplify, commit

The command file specified to run: `git add -A && git commit -m "feat: expanded 7-tab payroll spreadsheet"` after completion.

Also print at end:
- Suite object for run-tests-parallel.js
- Any new SQL needed (likely none — existing tables only, with graceful empty states for missing ones)
</context>

<next_action>
Start with: Invoke `superpowers:writing-plans` skill to write the implementation plan.
Then immediately write tests (TDD) — file: src/tests/unit/spreadsheetGenerator.test.js.
Key files to touch:
  - src/payroll/spreadsheetGenerator.js (ONLY this)
  - src/tests/unit/spreadsheetGenerator.test.js (ONLY this)
  - scripts/recalc.py (only if needs updating)
</next_action>
