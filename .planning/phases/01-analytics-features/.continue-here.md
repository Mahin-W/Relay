---
phase: 01-analytics-features
task: 0
total_tasks: 7
status: research_complete
last_updated: 2026-04-10T18:15:14.402Z
---

<current_state>
Step 0 (Research) is COMPLETE. All 8 research agents finished. No code has been written yet.
The repo is clean — no uncommitted files, no new files created.
Ready to begin Step 1: Build all 3 features in parallel.
</current_state>

<completed_work>

- Research Agent 1: Read src/index.js — documented all commands, imports, handler patterns
- Research Agent 2: Read src/setup/setupFlow.js + setupDb.js — wizard steps, DB patterns
- Research Agent 3: Read src/schedule/generateSchedule.js + reviewSchedule.js — return shapes, draft flow
- Research Agent 4: Read src/payroll/payCalculator.js + payDb.js — calculateWeeklyPayWithOT signature, payroll record schema
- Research Agent 5: Read src/db.js barrel + all DB modules — Supabase patterns, groupId usage
- Research Agent 6: Read src/tests/helpers/mocks.js — MockBot, MockDB, makeGroupMsg, makeDMMsg signatures
- Research Agent 7: Read src/tests/run-tests-parallel.js — Phase 1 (26 suites) and Phase 2 (9 suites) arrays
- Research Agent 8: Read all existing test files (37 unit, 4 integration, 1 e2e) — no overlap with new features
- Read routing files: dmRouter.js, groupRouter.js, commandRouter.js — exact insertion points identified
</completed_work>

<remaining_work>

- Task 1: Build Labor Cost % Tracker (src/analytics/laborCost.js + test) — TDD
- Task 2: Build Hours Budget Alert (src/analytics/budgetAlert.js + test) — TDD
- Task 3: Build Manager Shift Log (src/managerLog/shiftLog.js + shiftLogDb.js + test) — TDD
- Task 4: Wire all 3 features into src/index.js (read WIRING_TODO files, add imports + commands)
- Task 5: Wire budget alert into src/schedule/generateSchedule.js + reviewSchedule.js
- Task 6: Final validation — run all 3 test suites, defense-in-depth review
- Task 7: Print SQL + test suite objects for run-tests-parallel.js
</remaining_work>

<decisions_made>

- All 3 features can be built in parallel — they own separate files with no overlap
- No agent touches src/index.js or routing files during build phase — each leaves a WIRING_TODO.md
- Budget alert integrates into commandRouter.js /makeschedule flow (line 49-77) where draft is sent to manager
- DM log handler goes LAST in dmRouter.js (before line 156 fallback), only fires for managers with text > 10 chars
- Labor cost reads from existing payroll_records table via getPayrollForWeek() (payDb.js)
- Budget alert uses calculateProjectedLaborCost which mirrors payCalculator patterns but estimates from scheduled hours
</decisions_made>

<blockers>
None — research is complete and implementation plan is clear.
</blockers>

<context>
## Key Architecture Findings

### src/index.js command pattern:
```js
bot.onText(/^\/cmdname/, async (msg) => {
  if (!['group', 'supergroup'].includes(msg.chat.type)) return
  const groupId = String(msg.chat.id)
  const userId = msg.from?.id
  // auth check, then logic
})
```

### DB pattern (all modules):
```js
const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY)
export async function myFunc(groupId, ..., db = null) {
  if (db?.myFunc) return db.myFunc(groupId, ...)
  // real Supabase query
}
```

### generateSchedule returns:
`{ assignments, gaps, weekStart, scheduleId, clopenings, hoursIssues }`

### reviewSchedule.js key points:
- publishSchedule (line 92) calculates payroll after publishing
- commandRouter.js /makeschedule (line 49-77) sends draft to manager DM with clopening/hours warnings
- Budget alert should be inserted into the /makeschedule handler in commandRouter.js, appended to the draft DM

### payCalculator.js:
- `calculateWeeklyPay(assignments, shifts, roles, lateEvents)` — basic pay
- `calculateWeeklyPayWithOT(assignments, shifts, roles, overtimeSettings, lateEvents, partialCoverages)` — OT-aware
- Returns array of staff summaries with totalGrossPay, totalHours, etc.

### payDb.js:
- `getPayrollForWeek(groupId, weekStart, db)` — returns payroll records for a week
- `savePeriodPayroll(groupId, weekStart, staffPaySummaries, db)` — upserts payroll

### dmRouter.js insertion point:
- Log entry handler goes before line 156 (fallback message)
- After the trade offer check (line 140-154)
- Must check `getManagerGroup(userId)` to confirm sender is a manager

### MockBot/MockDB:
- MockBot: .sendMessage(), .sent, .last, .messagesTo(chatId), .assertSent(), .clear()
- MockDB: .seedStaff(), .seedShift(), .seedAssignment(), custom method mocking via property assignment
- makeGroupMsg({ text, chat, from }), makeDMMsg({ text, chat, from })

### New SQL tables needed:
1. weekly_revenue (group_id, week_start, revenue, total_labor_cost, labor_percent)
2. labor_budgets (group_id, weekly_budget, currency)
3. manager_log_entries (group_id, manager_id, entry_text, shift_name, day_of_week, week_start)
</context>

<next_action>
Start with: Dispatch 3 parallel build agents (TDD for each):
- Agent 1: Labor Cost % Tracker → src/analytics/laborCost.js + test
- Agent 2: Hours Budget Alert → src/analytics/budgetAlert.js + test  
- Agent 3: Manager Shift Log → src/managerLog/shiftLog.js + shiftLogDb.js + test

Each agent writes tests first (red), implements (green), runs simplify, creates WIRING_TODO.md, and commits.
Then wire all into index.js/routing files in Step 2.
</next_action>
